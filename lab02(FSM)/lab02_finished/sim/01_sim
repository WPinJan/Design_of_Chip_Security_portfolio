rm -rf simv
rm -rf simv.daidir

vcs -full64 -R -debug_acc+all+dmptf -debug_region+cell+encrypt\
    -cm line+branch+cond+fsm \
    -cm_hier dut.hier \
	-cm_noconst \
	-f vcs.f \
    -f sim.f \
    -l sim.log \
    "$@"
